{
  "name": "shopee-plub",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight Shopee voucher monitor that writes results to JSON via GitHub Actions.",
  "type": "commonjs",
  "scripts": {
    "check:vouchers": "node scripts/check-shopee-vouchers.js"
  },
  "engines": {
    "node": ">=20"
  }
}
